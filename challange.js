var output = [];
var count = 1;

function arrayChallange() {
  // write code here.
  output.push(count);
  // (comment) can use this.
  //   count = count + 1;
  // (comment) or this one.
  count++;
  console.log(output);
}
// (comment) to see the result copy "arrayChallange()" to google console and enter.
// (comment) when always enter will be auto add array.
